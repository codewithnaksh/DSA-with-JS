// Print all the characters of a string in a new line

const str = "Hello";

for (let i=0;i<str.length; i++){
    console.log(str.charAt(i));
}

// OR

const string = "Hi";
for (let char of string){
    console.log(char);
}